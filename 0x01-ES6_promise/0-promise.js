export default function getResponseFromAPI() {
  /* eslint-disable */
  return new Promise((resolve, reject) => {
      if (true) {
        resolve();
      } else {
        reject();
      }
  });
}
